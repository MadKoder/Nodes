# TODO signal/slots de struct generique
# structs:
# 	List<T> :
# 		list<T>	l
# 		signal onPushBack(T x)
# 		slot pushBack(T x):
# 			self.l.pushBack(x)
# 			self.onPushBack(x)
# functions:
# 	ref controlView(List<int> l):
# 		List<int>([x + 1 for x in l.l])
# 		# TODO : same with slot
# 		l.onPushBack => self.pushBack(x + 1)
# nodes:
# 	var l = List<int>([0, 1, 2])
structs:
	List :
		list<int>	l
		signal onPushFront(int x)
		slot pushFront(int x):
			# TODO : self.l.pushFront(x)
			self.l = pushFront(self.l, x)
			self.onPushFront(x)
functions:
	ref controlView(List l):
		List([x + 1 for x in l.l])
		l.onPushFront => self.pushFront(x + 1)
		# TODO : same with slot		
	ref controlView2(List l):
		List([x * 2 for x in l.l])
		l.onPushFront => self.pushFront(x * 2)
nodes:
	var l = List([0, 1, 2])	
	var v = controlView(l)
	var w = controlView2(l)
actions :
	tick :
		l.pushFront(3)
