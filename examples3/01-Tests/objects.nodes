structs:
	Vec2<T>:
		T x
		T y
		slot inc(Int i):
			x = x + i
	Rect<T>:
		Vec2<T> pos
		Vec2<T> size
nodes:
	var v = Vec2(0, 0)
	# var r = Rect(Vec2(0, 0), Vec2(10, 10))
	# var r2 = r
	def x = v.y
actions:
	tick(): 
		v.inc(1)
		# r.pos.x.inc(1)