class Klass (
    var x : int
) {
    def y = self.x + 10
    slot inc x : int do
        self.x = self.x + x
}
var o = new Klass(1)

var x = o.x
def y = o.x
def z = o.y

slot tick do
    o.x = 2

slot tick2 do
    o = new Klass(3)