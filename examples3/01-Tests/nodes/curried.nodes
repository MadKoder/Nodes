structs:
	Vec2<T>:
		T x
		T y
functions:
	# add : int -> int -> int
	inc x =
		x + 1
	add x y =
		(inc x) + y
	tupleAdd(x, y) =
		x + y
	typedAdd (x : int) (y : int) : int =
		x + y
	# add (x : int) (y : int) : int =
	# 	x + y
	# add<T> (x : T) (y : T) : T =
	# 	x + y
	# add<T>(T x, T y) =
	# 	x + y
nodes:
	var x = (add 0 1) + (add 1 2)
	var y = typedAdd 5 6
	var z = tupleAdd(0, 1)
	def v = Vec2 x y
	var l = [x + 1 for x in [1, 2]]