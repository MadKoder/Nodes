structs:
	Vec2<T>:
		T x
		T y
		slot inc(Int i):
			x = x + i
functions:
	add<T>(T x, T y) =
		x + y
nodes:
	var z = 1 + 2.
	var y = z
	def x = y + z
	def t = add(x, y)
	def v = Vec2(x, y)
actions:
	tick(Int i, Int j): 
		y = y + i
		subTick(0, y)
		simpleSignal()
	subTick(Int x, Int y):
		z = z + x + y 
	simpleSignal:
		y = y + 2
