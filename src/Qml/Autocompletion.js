var referenceStrings = {	"properties" : 	[		"activeFocus",		"anchors.baseline",		"anchors.baselineOffset",		"anchors.bottom",		"anchors.bottomMargin",		"anchors.centerIn",		"anchors.fill",		"anchors.horizontalCenter",		"anchors.horizontalCenterOffset",		"anchors.left",		"anchors.leftMargin",		"anchors.margins",		"anchors.mirrored",		"anchors.right",		"anchors.rightMargin",		"anchors.top",		"anchors.topMargin",		"anchors.verticalCenter",		"anchors.verticalCenterOffset",		"children",		"childrenRect.height",		"childrenRect.width",		"childrenRect.x",		"childrenRect.y",		"clip",		"data",		"focus",		"height",		"implicitHeight",		"implicitWidth",		"opacity",		"parent",		"resources",		"rotation",		"scale",		"state",		"states",		"transform",		"transformOrigin",		"transitions",		"visible",		"width",		"x",		"y",		"z",// Rectangle				"border.color",		"border.width",		"color",		"gradient",		"radius",		"smooth"	]}function getTextList(text, domain){	var strings = [];	var length = text.length;	if(length > 0)	{		var refs = referenceStrings[domain];		if(refs)		{			for(var i = 0; i < refs.length; i++)			{				if(refs[i].slice(0, length) == text)				{					strings.push(refs[i]);				}			}		}	}	return strings;}function getTextListFromAvailable(text, available){	var strings = [];	var length = text.length;	if(length > 0)	{		var refs = available;		if(refs)		{			for(var i = 0; i < refs.length; i++)			{				if(refs[i].slice(0, length) == text)				{					strings.push(refs[i]);				}			}		}	}	return strings;}